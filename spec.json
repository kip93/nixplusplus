{
    "type": 0,
    "enabled": true,
    "hidden": false,
    "nixexprinput": "src",
    "nixexprpath": "hydra/release.nix",
    "checkinterval": 900,
    "inputs": {
        "src": {
            "type": "git",
            "value": "ssh://git.kip93.net/nix++ main"
        },
        "nixpkgs": {
            "type": "git",
            "value": "https://github.com/NixOS/nixpkgs.git nixpkgs-unstable"
        }
    }
}
